# TODOdone2 — Verified completions (2026-03-23)

- `TODO.md:11` – Canonical names for Document RAG, Concept Search, Technician Intake, Service Sheet OCR, and Municipal Invoice Processing appear in `docs/02-architecture/document-retrieval-canonical-note.md:19-355`, so the naming publication is live.
- `TODO.md:12` – `backend/app/services/hybrid_query_service.py:334-374` now calls `concept_document_search` instead of the old `doc_rag_service`, which confirms the retrieval path fix.
- `TODO.md:242` – Phase 1 foundations (governance baseline + TOGAF artifacts) are documented in `docs/ai-governance/compliance-closure-report.md:38-58, 410-430`, showing the governance baseline, architecture board charter, and TOGAF evidence bundle as complete.
- `TODO.md:288` – The SOPS/age migration (replacing HashiCorp Vault) is captured in `infra/scripts/sops-rotate-key.sh:1-90`, which describes the 90-day cycle and rotation steps now in place.
- `TODO.md:289` – That same `infra/scripts/sops-rotate-key.sh:1-90` script automates the rotation task mentioned in the TODO entry.
- `TODO.md:290` – `.pre-commit-config.yaml:116-133` defines the `check-env-files` hook that blocks plaintext `.env` commits while allowing `.enc` files, proving the pre-commit update.
- `TODO.md:305` – The FSR questionnaire control mapping (Section 1 of `docs/ai-governance/fsr-questionnaire-control-mapping.md:1-41`) shows all domains linked to evidence, so the controls-to-questionnaire work is documented.
- `TODO.md:306` – The same mapping file (`docs/ai-governance/fsr-questionnaire-control-mapping.md:21-41`) lists the evidence directories that back each questionnaire domain, satisfying the evidence-documentation item.
- `TODO.md:309` – Architecture governance artifacts (including the board charter) live in `docs/architecture-repository/governance/architecture-board-charter.md:1-80`, confirming the architecture diagram package.
- `TODO.md:310` – Security policies such as the Application Security Policy are published in `docs/09-security/application-security-policy.md:1-75`.
- `TODO.md:311` – Audit logs are handled by `backend/app/services/audit_logger.py:1-80`, which encrypts and rotates `audit_log.json` while shipping structured JSON for SIEM ingestion.
- `TODO.md:427` – Equipment repository defines `_LIST_COLUMNS`/`_DETAIL_COLUMNS` to restrict Supabase selects (`backend/app/database/repositories/equipment_repository.py:16-200`).
- `TODO.md:428` – `backend/app/services/cache_service.py:300-360` records `sentinel_db_query_duration_seconds` for each repository/method, matching the Prometheus histogram requirement.
- `TODO.md:432` – Redis caching is wired through `backend/app/services/cache_service.py:300-380`, confirming cache usage for buildings, equipment, alerts, and predictions.
- `TODO.md:433` – Cache invalidation helpers in `backend/app/services/cache_service.py:420-460` are invoked on building/equipment/alert/prediction writes, proving cache invalidation coverage.
- `TODO.md:434` – TTL presets (“SEMI_STATIC = 300”, “DYNAMIC = 60”) live in `backend/app/services/cache_service.py:44-90` and match the TTL expectations.
- `TODO.md:435` – `backend/app/services/cache_service.py:300-360` also increments `sentinel_cache_operations_total` and updates `sentinel_cache_hit_rate_percent` for hits/misses, covering the cache metrics.
- `TODO.md:439` – The `track_query` context manager logs slow queries (`backend/app/services/cache_service.py:460-520`), providing `SLOW_QUERY` warnings for durations ≥0.5 s.
- `TODO.md:440` – Grafana panels are defined in `infrastructure/grafana/provisioning/dashboards/sentinel-db-performance.json:1-80`, which includes the performance dashboard referenced in the item.
- `TODO.md:441` – The Supabase performance runbook (`docs/10-operations/supabase-performance-runbook.md:1-120`) documents the dashboard changes and operational procedures.
- `TODO.md:751` – The new skill lives in `skills/sentinel-bms/SKILL.md:1-60`, which includes the required front matter and workflow guidance.
- `TODO.md:752` – The same skill description (`skills/sentinel-bms/SKILL.md:3-60`) spells out the trigger phrases covering buildings, equipment, alerts, work orders, DALI, load shedding, and energy.
- `TODO.md:753` – Fault confidence tiers (0.60+, 0.40–0.59, <0.40) appear at `skills/sentinel-bms/SKILL.md:75-82`.
- `TODO.md:754` – City Power TOU, solar offset, and BESS charging context are described in `skills/sentinel-bms/SKILL.md:110-118` under the “Energy Context” section.
- `TODO.md:755` – Module awareness guidance (base vs. add-on capabilities) is explained in `skills/sentinel-bms/SKILL.md:121-135`.
- `TODO.md:756` – The data-agnostic principle (“never describe data as demo/mock/simulated/test”) is enforced at `skills/sentinel-bms/SKILL.md:31-36`.
- `TODO.md:887` – `backend/ml/monitoring/drift.py:220-260` drives the `sentinel_model_drift_alerts` gauge inside `_detect_all_drift()` so the drift metric is wired to `_run_drift_detection()`.
- `TODO.md:896` – Background ML retraining architecture is documented in `docs/02-architecture/background-ml-retraining.md:1-80`, covering the scheduler, worker, and retraining policy.
- `TODO.md:1405` – `backend/app/middleware/request_metrics.py:1-70` defines the RequestMetricsMiddleware that labels all HTTP routes with method, normalized path, status code, and latency.
- `TODO.md:1408` – `backend/app/services/chat_tools.py:4700-4785` increments `sentinel_tool_calls_total`/`sentinel_tool_call_duration_seconds` and logs tool successes/errors, providing the tool-call instrumentation.
- `TODO.md:1410` – The same chat tools file (`backend/app/services/chat_tools.py:4700-4785`) wires the Prometheus counters/ histograms into `governance_metrics.record_tool_error` and the final `finally` block.
- `TODO.md:1414` – The monitoring stack rollout (Prometheus, Grafana, Loki, Promtail, Node Exporter) is described in `docs/10-operations/monitoring-stack.md:150-210`.
- `TODO.md:1415` – Tests covering RequestMetrics and metrics endpoints live in `backend/tests/middleware/test_request_metrics.py:1-80` and `backend/tests/api/test_metrics_endpoint.py:1-120`.
- `TODO.md:1427` – The latency-per-endpoint Grafana panel is part of the monitoring-stack dashboard (`docs/10-operations/monitoring-stack.md:190-220`).
- `TODO.md:1710` – `backend/app/middleware/request_metrics.py:30-70` increments `sentinel_http_requests_total` per route, satisfying the counter wiring.
- `TODO.md:1711` – The same middleware file (`backend/app/middleware/request_metrics.py:30-70`) records durations in `sentinel_http_request_duration_seconds`.
- `TODO.md:1712` – `backend/ml/monitoring/drift.py:220-260` also powers the `sentinel_model_drift_alerts` gauge, as required by the scheduler.
- `TODO.md:1713` – `backend/app/services/chat_tools.py:4700-4785` emits `sentinel_tool_calls_total` and `sentinel_tool_call_duration_seconds` for every tool invocation.
- `TODO.md:1723` – `/api/governance/drift-scores` is implemented in `backend/app/api/governance_metrics_api.py:137-166` and feeds Grafana plus alerts when `sentinel_model_drift_score` exceeds thresholds.
- `TODO.md:1913` – The legacy gateway was replaced by the vendored Sentry gateway (`sentry-gateway/`), as noted in `docs/04-features/phase-implementations.md:22-50` and by the presence of the gateway directory.
- `TODO.md:2036` – `backend/app/services/system_health_service.py:1-160` probes Supabase, Redis, Event Bus, n8n, ServiceNow, Notifications, and Device Manager with weighted scoring, matching the dashboard requirement; see also `docs/04-features/system-health-diagnostics.md:1-120`.
- `TODO.md:2037` – The Redis probe inside `backend/app/services/system_health_service.py:120-180` makes the health check depend on the `redis` package, which triggered the systemd fix noted in the TODO entry.
