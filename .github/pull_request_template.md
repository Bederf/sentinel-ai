## Summary

<!-- What does this change do, and why? -->

## Checklist

- [ ] `ruff format --check .` / `ruff check .` pass (backend changes)
- [ ] `npm run lint` / `npx tsc --noEmit` pass (frontend changes)
- [ ] Relevant unit tests added/updated and passing
- [ ] No secrets, credentials, or `.env*` values committed
- [ ] Large files (>~800 lines) split rather than grown, where practical

## Test plan

<!-- How did you verify this? Commands run, manual steps, screenshots if UI. -->
