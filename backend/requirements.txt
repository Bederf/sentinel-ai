# BMS Intelligence Backend Dependencies

# Web Framework
fastapi>=0.140.13
uvicorn[standard]==0.52.0

# Settings and Validation
pydantic[email]>=2.13.4
pydantic-settings>=2.14.2

# HTTP Client (async preferred over requests)
httpx>=0.28.1,<1.0.0
requests>=2.31.0  # Fallback for legacy integrations

# AI/LLM
anthropic>=0.39.0
voyageai==0.4.1

# Database
supabase>=2.31.0
psycopg2-binary==2.9.9
redis>=5.0.0
asyncpg>=0.29.0

# Time-Series Database
influxdb-client>=1.38.0

# ML Feature Store
pandas>=2.0.0
pyarrow>=14.0.0

# ML Models (Fault Classification, Anomaly Detection)
scikit-learn>=1.3.0
joblib>=1.3.0
tensorflow>=2.16.0

# Data Migration
ijson>=3.2.0
tqdm>=4.66.0

# Testing
pytest>=9.0.3
pytest-asyncio>=0.25.0
pytest-cov>=4.0.0
pytest-timeout>=2.1.0
ruff>=0.15.1

# Background Tasks
apscheduler==3.10.4
filelock>=3.13.0  # Concurrent archival safety (Phase 168-03)

# Web Scraping (Equipment Lookup)
aiohttp>=3.14.0
beautifulsoup4>=4.12.0
lxml>=6.1.0

# Report Generation
reportlab>=4.0.0

# PDF Extraction (Municipal Billing)
pymupdf==1.23.22
pdfplumber==0.11.9
PyPDF2==3.0.1  # PDF text extraction for document upload
python-docx>=1.1.0  # DOCX text extraction for document upload

# Document OCR & Layout Analysis (Phase 181)
# Docling: better scanned-PDF OCR, table extraction, key-value pair detection
# Import: from docling.document_converter import DocumentConverter
docling>=2.94.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0  # CPU-only build (extra-index-url ensures CPU wheels)

# Image Processing & OCR (Core Infrastructure)
# ⚠️  REQUIRES SYSTEM BINARY: tesseract-ocr
# Used in: floor plan sanitization (Phase A), work order technician pipeline (Phase 41),
# equipment vision processing, service sheet data entry
# See: docs/01-setup/ocr-setup-guide.md
opencv-python-headless>=4.8.0  # Headless version for servers (no GUI)
Pillow>=10.0.0  # Image manipulation
pytesseract>=0.3.10  # OCR text detection (⚠️ REQUIRES tesseract-ocr system binary)
python-multipart>=0.0.27  # For file uploads in endpoints

# BACnet/IP Protocol (Niagara Integration)
BAC0>=24.3.25

# MFA/TOTP (Phase 58.1 FSR Compliance - Domain 4.6)
pyotp>=2.9.0

# Rate Limiting (Phase 58.3 Security Hardening)
slowapi>=0.1.9
bcrypt>=4.2.0

# Encryption (Phase 1b FSR Compliance - Cryptography)
cryptography>=49.0.0  # AES-256 encryption for audit logs and PII at rest
defusedxml>=0.7.1     # Secure XML parsing (XXE/bomb protection)

# DXF File Parsing (Phase B: Digital Twin Builder)
# AutoCAD/CAD drawing support for equipment extraction
ezdxf>=1.3.0                   # Read/write AutoCAD DXF files (all versions R12-2024)
numpy>=1.24.0                  # Numeric calculations for geometry transformations

# Agent Framework (LangGraph)
langgraph>=0.2.0               # StateGraph with checkpointing for multi-turn agents

# Prometheus Metrics (Phase 114 - AI Governance Observability)
prometheus_client>=0.20.0      # Prometheus text exposition format for /metrics endpoint

# Solar+BESS Optimization (v26.0)
ortools>=9.8.0                 # Google OR-Tools CP-SAT solver for MIP dispatch optimization
pymodbus>=3.6.0                # Modbus TCP client for Huawei LUNA2000 BESS register writes

# Email Reply Service (Phase 131.2b — threaded SMTP replies)
aiosmtplib>=2.0.0              # Async SMTP client for RFC 822 threaded email replies

# MQTT Client (Fuel Tank + Space Occupancy)
paho-mqtt>=1.6.1               # Eclipse Paho MQTT client for ESP32/Modbus-MQTT bridge ingestion

# MCP Server & Schema Validation
jsonschema>=4.20.0             # JSON Schema validation for MCP tool inputs
email-validator>=2.0.0         # Email address validation (Pydantic[email] runtime dep)

# Visitor Management QR Codes (Phase 176)
qrcode[pil]>=7.4.0             # QR code generation (with Pillow for image output)
segno>=1.5.0                   # QR code library with SVG/artistic support for demo variants
qrcode-artistic>=1.0.0         # Artistic QR code generation (background images)

# Twilio WhatsApp Integration (Phase 176)
twilio>=8.0.0                  # WhatsApp webhook responses for visit confirmation

# Microsoft Authentication (Phase 176 — Outlook/Graph API)
msal>=1.26.0                   # Microsoft Authentication Library for Outlook calendar service

# Fuzzy String Matching (Phase 180 — Asset ID Resolver)
rapidfuzz>=3.6.0               # token_set_ratio for equipment description fuzzy matching

# ODS-E (Open Data Schema for Energy) Export (Phase 209)
# Validation library for ODS-E v0.4.0 energy data export
odse>=0.4.0                    # ODS-E schema validation for Asoba eSUMS integration

# KNX/IP Protocol (Wave 1 - SENTINEL BMS Adapter)
xknx>=2.12.0
infisical-python>=2.0.0
