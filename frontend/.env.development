# Local backend - development only
VITE_API_URL=http://localhost:9095
VITE_DEBUG=false

# OpenAI Realtime-2 voice pipeline (Path C surgical — replaces ElevenLabs STT only)
# Default false — flip to true after validation gates pass
VITE_REALTIME_VOICE_ENABLED=true
